[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-dir]
"ksef" = "ksef"

[tool.setuptools.package-data]
"*" = ["*.xml"]

[project]
name = "ksef2.0_python"
version = "1.1.0"
dependencies = [
  "requests",
  "cryptography",
  "python-dateutil",
  "endesive",
  "lxml",
  "signxml",
  "asn1crypto",
  "xml_konwerter @ git+https://github.com/stanislawbartkowski/xml_konwerter.git"
]
requires-python = ">=3.10"
authors = [
  {name = "Stanisław Bartkowski", email = "stanislawbartkowski@gmail.com"},
]
readme = "README.md"
